<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ $subject }}</title>
</head>
<body>
    <h1>{{ $subject }}</h1>
    <p>{{ $mensagem }}</p>
    <a href="{{ $buttonUrl }}">Click here</a> to proceed.
</body>
</html>
